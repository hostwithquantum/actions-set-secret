{
  "name": "secret-updater",
  "version": "2.0.1",
  "repository": "https://github.com/hostwithquantum/actions-set-secret",
  "license": "MIT License",
  "scripts": {
    "lint": "eslint .",
    "build": "ncc build index.js -o dist",
    "test": "node --test 'tests/**/*.test.js'"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^9.0.0",
    "@octokit/core": "^7.0.6",
    "eslint": "^10.0.3",
    "libsodium-wrappers": "^0.7.15"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "eslint-config-strongloop": "^2.1.0",
    "eslint-plugin-jsdoc": "^46.9.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/*.js": [
      "eslint . --fix"
    ]
  }
}
